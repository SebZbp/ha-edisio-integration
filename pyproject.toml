[project]
name = "ha-edisio-integration"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "homeassistant>=2024.12.5",
    "pyserial-asyncio>=0.6",
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
]
